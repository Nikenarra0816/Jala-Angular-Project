import { JalaButtonIconDirective } from './jala-button-icon.directive';

describe( 'JalaButtonPlusIconDirective', () => {
	it( 'should create an instance', () => {
		// const directive = new JalaButtonIconDirective();
		// expect(directive).toBeTruthy();
		expect( true ).toBeTruthy();
	} );
} );
