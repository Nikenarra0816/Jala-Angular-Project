import { JalaButtonInlineIconDirective } from './jala-button-inline-icon.directive';

describe('JalaButtonInlineIconDirective', () => {
  it('should create an instance', () => {
    const directive = new JalaButtonInlineIconDirective();
    expect(directive).toBeTruthy();
  });
});
