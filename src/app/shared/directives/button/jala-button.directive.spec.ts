import { JalaButtonDirective } from './jala-button.directive';

describe('JalaButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new JalaButtonDirective();
    expect(directive).toBeTruthy();
  });
});
