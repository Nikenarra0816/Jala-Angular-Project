import { FormFieldInputGreenDirective } from './form-field-input-green.directive';

describe('FormFieldInputGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldInputGreenDirective();
    expect(directive).toBeTruthy();
  });
});
