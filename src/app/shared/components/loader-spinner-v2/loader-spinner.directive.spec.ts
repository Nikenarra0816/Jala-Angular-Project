import { LoaderSpinnerDirective } from './loader-spinner.directive';

describe('LoaderSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new LoaderSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
